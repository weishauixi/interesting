# Cloudflare Pages 部署指南 🚀

Cloudflare Pages 提供无限带宽，全球 CDN 加速，非常适合部署您的马年祝福平台！

## 📋 前提准备

1. GitHub 账号（用于托管代码）
2. Cloudflare 账号（免费）
3. Supabase 账号（如果使用云端存储）

---

## 方法一：通过 GitHub 自动部署（推荐）

### 第 1 步：推送到 GitHub

1. 在 GitHub 创建新仓库：`horse-year-greetings`

2. 在项目根目录执行：
```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 马年春节祝福平台 🐴🧧"

# 添加远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/horse-year-greetings.git

# 推送到 GitHub
git push -u origin main
```

### 第 2 步：部署到 Cloudflare Pages

1. 访问：https://pages.cloudflare.com/
2. 点击 "Create a project"
3. 选择 "Connect to Git"
4. 授权 Cloudflare 访问您的 GitHub
5. 选择 `horse-year-greetings` 仓库
6. 配置构建设置：

```
构建命令：npm run build
构建输出目录：dist
Node.js 版本：18（或最新）
```

7. 点击 "Save and Deploy"
8. 等待 1-2 分钟，部署完成！

### 第 3 步：配置环境变量

1. 在 Cloudflare Pages 项目中
2. 进入 Settings → Environment variables
3. 添加以下变量：

```
VITE_SUPABASE_URL = your-supabase-url
VITE_SUPABASE_ANON_KEY = your-anon-key
```

4. 保存后重新部署

### 第 4 步：访问您的网站

部署成功后，Cloudflare 会给您一个免费域名：
```
https://your-project.pages.dev
```

---

## 方法二：手动上传（无需 Git）

### 第 1 步：构建项目

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build
```

### 第 2 步：上传到 Cloudflare Pages

1. 访问：https://pages.cloudflare.com/
2. 点击 "Create a project"
3. 选择 "Upload assets"
4. 将 `dist` 文件夹内的所有文件拖拽上传
5. 点击 "Deploy site"

---

## ⚙️ 配置自定义域名（可选）

### 使用您自己的域名

1. 在 Cloudflare Pages 项目设置中
2. 选择 "Custom domains"
3. 添加您的域名（如 `newyear.yourdomain.com`）
4. 按照提示修改 DNS 记录

### 使用 Cloudflare 免费域名

Cloudflare Pages 会自动提供：
```
your-project.pages.dev
```

---

## 🔄 自动部署（Git 集成）

配置完成后，每次您推送代码到 GitHub：

```bash
git add .
git commit -m "Update: 新功能"
git push
```

Cloudflare Pages 会**自动**：
1. 检测到代码更新
2. 运行 `npm run build`
3. 部署新版本
4. 更新网站

---

## 📊 Cloudflare Pages 免费额度

| 项目 | 免费额度 |
|------|---------|
| **带宽** | ✅ **无限** |
| **请求数** | 无限 |
| **构建次数** | 500 次/月 |
| **站点数量** | 无限 |
| **域名** | 无限自定义域名 |
| **SSL 证书** | 免费自动 HTTPS |
| **CDN** | 全球 200+ 节点 |

---

## 🎯 快速部署命令

```bash
# 完整流程
npm install              # 安装依赖
npm run build           # 构建
# 然后在 Cloudflare Pages 控制台上传 dist 文件夹
```

---

## 💡 提示

### 首次部署建议
1. ✅ 先在本地测试确保功能正常
2. ✅ 检查 `.env` 文件中的 Supabase 配置
3. ✅ 构建前确保 `USE_LOCAL_STORAGE = false`（如果使用 Supabase）

### 环境变量配置
Cloudflare Pages 需要在控制台配置环境变量，不能直接读取 `.env` 文件。

### 排查问题
如果部署后无法访问：
1. 检查构建日志是否有错误
2. 检查环境变量是否正确配置
3. 查看浏览器控制台的错误信息

---

## 🆚 Cloudflare Pages vs 其他平台

| 特性 | Cloudflare Pages | Netlify | Vercel | GitHub Pages |
|------|-----------------|---------|--------|--------------|
| **带宽** | ✅ 无限 | 100GB/月 | 100GB/月 | 100GB/月 |
| **国内速度** | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| **构建次数** | 500次/月 | 300分钟/月 | 6000分钟/月 | 无限 |
| **配置难度** | ⭐ 简单 | ⭐ 简单 | ⭐ 简单 | ⭐⭐ 中等 |

---

## 🎉 部署成功后

您的马年祝福平台将在全球可访问：
- 🌏 亚洲节点快速访问
- 🚀 Cloudflare CDN 加速
- 🔒 免费 SSL 证书
- 📊 实时分析数据

---

## 📞 需要帮助？

- Cloudflare 文档：https://developers.cloudflare.com/pages/
- 社区论坛：https://community.cloudflare.com/

祝您部署顺利！🎊🐴
